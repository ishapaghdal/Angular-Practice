import { TempreturePipe } from './tempreture.pipe';

describe('TempreturePipe', () => {
  it('create an instance', () => {
    const pipe = new TempreturePipe();
    expect(pipe).toBeTruthy();
  });
});
